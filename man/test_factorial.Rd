% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_factorial.R
\name{test_factorial}
\alias{test_factorial}
\title{Run a factorial ANOVA workflow}
\usage{
test_factorial(
  formula,
  data,
  factors = NULL,
  alpha = 0.05,
  type = 2,
  plot = TRUE,
  na.rm = TRUE
)
}
\arguments{
\item{formula}{A formula such as \code{outcome ~ factor1 * factor2}, or a data frame when using data-first style.}

\item{data}{A data frame, or the outcome column when using data-first style.}

\item{factors}{Factor columns selected with tidyselect syntax. Optional when using formula style.}

\item{alpha}{Significance level.}

\item{type}{Sums-of-squares type: \code{1} (sequential, base \code{aov()}), \code{2}, or
\code{3} (via \code{car::Anova()}, required for \code{type = 2}/\code{3}). For unbalanced
designs these can give materially different p-values; \code{type = 2} is a
reasonable default when there is no strong prior reason to test factors
in a particular order. \code{type = 3} requires sum-to-zero contrasts, which
this function sets automatically.}

\item{plot}{Logical; include a ggplot object.}

\item{na.rm}{Logical; remove missing values.}
}
\value{
A \code{testflow} object with class \code{testflow_factorial}. The object is a
list containing the cleaned data, descriptive statistics, residual and
variance assumption checks, recommended factorial ANOVA, primary ANOVA term
result with null hypothesis, ANOVA table, effect size, optional \code{ggplot},
original call, and report text.
}
\description{
Run a factorial ANOVA workflow
}
\references{
Fisher, R. A. (1925). \emph{Statistical Methods for Research Workers}.
Oliver and Boyd.

Cohen, J. (1988). \emph{Statistical Power Analysis for the Behavioral
Sciences} (2nd ed.). Lawrence Erlbaum.
}
